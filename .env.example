# AI Debate Arena — Environment Variables

# OpenAI
OPENAI_API_KEY=

# DeepSeek
DEEPSEEK_API_KEY=

# Optional later
OPENROUTER_API_KEY=

# Deep Debate web search (https://api-dashboard.search.brave.com)
# Metered search. Also set this in Vercel env vars for production.
BRAVE_SEARCH_API_KEY=
# Optional: which search engine to use (default "brave") and the per-query fee
# reserved before each search (default/floor $0.005; raise if your plan costs more).
SEARCH_PROVIDER=
SEARCH_COST_USD=
# All fighters use app-managed search so its fee can be reserved explicitly.

# App-level protection for private beta
APP_ACCESS_PASSWORD=

# Cost & abuse protection (per-IP rate limits + daily spend caps). Enforced in
# Postgres via Supabase — apply pending migrations through 0015 using the runner.
# All optional; the defaults below apply if unset. Needs Supabase configured.
RL_TURN_PER_MIN=30         # max /api/debate/turn per IP per window (a Blitz match fires 8 in a burst)
RL_VERDICT_PER_MIN=4       # max /api/debate/verdict requests per IP per window
RL_OG_PER_MIN=30           # max /api/og image renders per IP per window
RL_WINDOW_SECONDS=60       # rate-limit window length (seconds)
SPEND_GLOBAL_DAILY_USD=15  # daily booked spend across all users (USD)
SPEND_IP_DAILY_USD=3       # daily booked spend per IP (USD)
# Reservations use conservative token-price estimates, not provider invoice guarantees.
# Disabled voice feature: conservative per-request booking if later enabled.
TTS_MAX_REQUEST_USD=0.5

# Auth + profiles + match persistence (Supabase — see docs/19).
# URL + publishable (anon) key are PUBLIC (row-level security protects data);
# also set these two in Vercel env vars (Production + Preview) for the live site.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
# SERVER-ONLY, never expose / never NEXT_PUBLIC. REQUIRED in production even with
# coins off: it owns generation state and atomic spend reservations, signs coins,
# and is the caller for
# the service-role-only rate-limit/spend RPCs, and enables analytics + /admin.
SUPABASE_SERVICE_ROLE_KEY=

# Optional override for the coin charge-key HMAC secret. Defaults to
# SUPABASE_SERVICE_ROLE_KEY when unset; set a distinct strong random value to
# rotate it independently.
COIN_CHARGE_SECRET=

# App URL
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Signs shareable verdicts so forged /s?d= links show as "unverified" (critical #2).
# Any strong random value (e.g. `openssl rand -hex 32`). Empty disables signing.
SHARE_SECRET=
