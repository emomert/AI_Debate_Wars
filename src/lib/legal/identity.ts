/**
 * Operator / data-controller identity, surfaced on the Terms, Privacy Policy,
 * /contact page and footer (audit criticals #3 contact channel, #4 controller
 * identity + governing law).
 *
 * ⚠️ THESE ARE PLACEHOLDERS — replace all three with real values before a public
 * launch. They're centralized here so every surface updates from this one file.
 */

/** Legal operator named as the data controller (a company, or your own name). */
export const OPERATOR_NAME = "[OPERATOR NAME — placeholder, set before launch]";

/** A real, monitored contact address for support + GDPR/KVKK data requests. */
export const CONTACT_EMAIL = "contact@example.com"; // placeholder — replace before launch

/** Jurisdiction whose law governs the Terms (e.g. "Turkey"). */
export const GOVERNING_LAW = "[JURISDICTION — placeholder, set before launch]";
